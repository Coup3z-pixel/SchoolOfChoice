#ifndef SOLVER_H
#define SOLVER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "partalloc.h"

struct partial_alloc GCPS_schools_solver(struct sch_ch_prob* my_scp);

#endif /* SOLVER_H */
